query Projects {
  projects(orderBy: createdAt_DESC) {
    id
    name
    description
    image {
      url
    }
    githubUrl
    websiteUrl
    technologies {
      id
      name
      websiteUrl
      image {
        url
      }
      highlightColor
    }
    isFeatured
  }
}