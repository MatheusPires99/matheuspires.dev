query Projects {
  projects(orderBy: publishedAt_DESC) {
    id
    name
    description
    image {
      url
    }
    githubUrl
    websiteUrl
    technologies {
      id
      name
      websiteUrl
      image {
        url
      }
      highlightColor
    }
    isFeatured
  }
}