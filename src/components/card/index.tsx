import { styled } from "@/styles";

export const Card = styled("div", {
  padding: "1.5rem",
  backgroundColor: "$accent-shape",
  boxShadow: "0px 10px 30px -15px rgba(18, 18, 20, 0.7)",
  borderRadius: "$sm",
});
