import { styled } from "@/styles";

export const Card = styled("div", {
  backgroundColor: "$shape",
  boxShadow: "$md",
  borderRadius: "$sm",
  overflow: "hidden",
  lineHeight: "1.5",
  padding: "1.5rem",
});
